/// <reference types="jest" />

import { renderWithProviders } from '../setupTests';

describe('AuthService', () => {
  it('should render', () => {
    // در صورت لزوم می‌توانید رندر کامپوننت سرویس را اینجا انجام دهید
    expect(true).toBeTruthy();
  });
});